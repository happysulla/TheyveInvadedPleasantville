<Window x:Class="PleasantvilleGame.JoinGameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Join Game"
        Width="420"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Text="Server Address:" />
        <TextBox Grid.Row="0" Grid.Column="1" Name="ServerAddressTextBox" Margin="0,0,0,8" MinWidth="260" Text="http://127.0.0.1:50051" />

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Text="Session Id:" />
        <TextBox Grid.Row="1" Grid.Column="1" Name="SessionIdTextBox" Margin="0,0,0,8" />

        <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Text="Join Code:" />
        <TextBox Grid.Row="2" Grid.Column="1" Name="JoinCodeTextBox" Margin="0,0,0,8" />

        <TextBlock Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,12" TextWrapping="Wrap">
            The first scaffold joins as the Town side and fetches a role-filtered visible game snapshot from the host.
        </TextBlock>

        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="80" Margin="0,0,8,0" IsDefault="True" Click="Ok_Click">OK</Button>
            <Button Width="80" IsCancel="True">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>
